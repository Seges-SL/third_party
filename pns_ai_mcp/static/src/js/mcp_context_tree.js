/** @odoo-module **/
/**
 * mcp_context_tree.js  (OWL 2 – Odoo 17+)
 *
 * Context tree action buttons (Stats, Restore, Export, Import) are implemented
 * as ir.actions.server with binding_model_id on pns_ai_mcp.mcp_context.
 * The Stats dialog is triggered via a server action that opens a wizard.
 *
 * Legacy Odoo 14 version: mcp_context_tree_v14.js (loaded via assets.xml)
 */
// No-op module — functionality moved to server-side action bindings
