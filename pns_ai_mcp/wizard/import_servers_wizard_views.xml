<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_import_servers_wizard_form" model="ir.ui.view">
        <field name="name">pns.ai.mcp.import.servers.wizard.form</field>
        <field name="model">pns_ai_mcp.import_servers_wizard</field>
        <field name="arch" type="xml">
            <form string="Import AI Providers">
                <sheet>
                    <div class="alert alert-info" role="alert" >
                        <p>Upload a previously exported JSON file.</p>
                        <p>If "Replace existing" is checked, providers with the same name will be updated.</p>
                    </div>
                    <group >
                        <field name="json_file" filename="filename"/>
                        <field name="filename" invisible="1"/>
                        <field name="replace_existing"/>
                    </group>
</sheet>
                <footer>
                    <button name="action_import" string="Import" type="object" class="btn-primary" />
                    <button string="Cancel" class="btn-secondary" special="cancel" />
</footer>
            </form>
        </field>
    </record>

    <record id="action_import_servers_wizard" model="ir.actions.act_window">
        <field name="name">Import AI Providers (JSON)</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">pns_ai_mcp.import_servers_wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
